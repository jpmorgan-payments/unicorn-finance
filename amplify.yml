version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - cd app
        - cd client
        - pnpm install
    build:
      commands:
        - pnpm run build

  artifacts:
    baseDirectory: /app/client/dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
